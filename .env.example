# =============================================================================
# HOMELAB INFRASTRUCTURE SECRETS
# =============================================================================
# All secrets for this homelab project are centralized in this file.
# Copy this file to .env and fill in your actual values.
# Never commit .env to git (.gitignored for safety).

# =============================================================================
# ANSIBLE VAULT PASSWORD
# =============================================================================
# Used to encrypt/decrypt secrets: WireGuard keys, Let's Encrypt certificate backups
# Generate a strong password (20+ random characters recommended)
ANSIBLE_VAULT_PASSWORD=your-secure-vault-password-here

# =============================================================================
# TERRAFORM VARIABLES (TF_VAR_* prefix required)
# =============================================================================
# Terraform automatically reads environment variables with TF_VAR_* prefix.
# These replace the deprecated terraform/terraform.tfvars file.

# Hetzner Cloud API Token
# Get from: https://console.hetzner.cloud/ -> Project -> Security -> API Tokens
# Required permissions: Read & Write
TF_VAR_hcloud_token=YOUR_HETZNER_API_TOKEN_HERE

# Cloudflare API Token (shared with Ansible ddclient)
# Get from: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Zone > DNS > Edit
# This token is used by both Terraform (DNS creation) and Ansible (ddclient)
TF_VAR_cloudflare_api_token=YOUR_CLOUDFLARE_API_TOKEN_HERE

# Cloudflare Zone ID
# Find in: https://dash.cloudflare.com/ -> Select your domain -> Overview (right sidebar)
TF_VAR_cloudflare_zone_id=YOUR_CLOUDFLARE_ZONE_ID_HERE

# Your Domain Name
# Example: "example.com" or "yourdomain.com"
TF_VAR_domain=yourdomain.com

# SSH Public Key for Bastion Access
# Generate with: ssh-keygen -t ed25519 -C "bastion-homelab"
# Use the contents of ~/.ssh/id_ed25519.pub
TF_VAR_ssh_public_key="ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAA... your-email@example.com"

# Optional Terraform Variables (uncomment to override defaults)
# Plex subdomain (default: "plex" -> creates plex.yourdomain.com)
# TF_VAR_plex_subdomain=plex

# VPS server name (default: "bastion-plex")
# TF_VAR_server_name=bastion-plex

# VPS instance type (default: "cpx11" - 2 vCPU, 2GB RAM, ~$4-5/month)
# TF_VAR_server_type=cpx11

# VPS data center location (default: "ash" - Ashburn, VA)
# Options: ash (Ashburn, VA), hil (Hillsboro, OR), nbg1 (Nuremberg), fsn1 (Falkenstein)
# TF_VAR_server_location=ash

# VPS OS image (default: "ubuntu-24.04")
# TF_VAR_server_image=ubuntu-24.04

# =============================================================================
# ANSIBLE VARIABLES
# =============================================================================

# Cloudflare Zone (domain name) for ddclient
# This should match TF_VAR_domain above
# Used by Ansible ddclient role for dynamic DNS updates
CLOUDFLARE_ZONE=yourdomain.com

# =============================================================================
# TASK MASTER AI (Optional - for project management)
# =============================================================================
# Uncomment and add your API key for the provider you want to use
# ANTHROPIC_API_KEY=your-anthropic-api-key-here
# OPENAI_API_KEY=your-openai-api-key-here
# OPENROUTER_API_KEY=your-openrouter-api-key-here
